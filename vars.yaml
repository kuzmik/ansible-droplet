---
# DO API v2 settings
do_api_token: ~

# DO SSH key settings
do_ssh_name: root
do_ssh_pub_key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"
do_ssh_private_key: "~/.ssh/id_rsa"
do_ssh_key_fingerprint: 77:2d:f3:38:cd:47:40:2d:1b:1f:88:48:97:ce:97:e2

# DO droplet settings
do_region: nyc3
do_size: 512mb
do_image: "debian-9-x64"
do_droplet_name: "debian-512mb-nyc3-02"

# System user to create
ssh_user: "nick"
ssh_groups: "sudo"
ssh_pub_key: "{{ lookup('file', '~/.ssh/id_rsa.pub') }}"

# Admin Email
admin_email: nick@kuzmik.org

# System config vars
ssh_port: 8008
